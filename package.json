{
  "private": true,
  "scripts": {
    "preinstall": "typesync || :",
    "postinstall": "dotnet tool restore && elm-tooling install",
    "fable": "dotnet fable src/App -o src/generated/App-js",
    "start": "npm run fable -- --watch --run vite",
    "build": "npm run fable && tsc --outDir ./dist --declaration --noEmit false",
    "build2": "gulp"
  },
  "devDependencies": {
    "@types/gulp": "^4.0.10",
    "@types/react": "^18.2.6",
    "@types/react-dom": "^18.2.4",
    "elm-tooling": "^1.14.0",
    "esbuild": "^0.17.18",
    "esbuild-plugin-elm": "^0.0.12",
    "esbuild-runner": "^2.2.2",
    "gulp": "^4.0.2",
    "gulp-esbuild": "^0.11.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "typesync": "^0.11.0"
  },
  "dependencies": {
    "fable-library": "^0.1.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "version": "1.0.0"
}
